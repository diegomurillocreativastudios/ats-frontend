export const metadata = {
  title: { absolute: "ATS | Etapas" },
  description: "Gestión de etapas del proceso de reclutamiento",
};

export default function EtapasLayout({ children }) {
  return children;
}
