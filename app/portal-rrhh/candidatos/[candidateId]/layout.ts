export const metadata = {
  title: { absolute: "ATS | Portal RRHH | Candidato" },
  description: "Perfil detallado del candidato en el portal RRHH",
};

export default function CandidatoDetalleLayout({ children }) {
  return children;
}
