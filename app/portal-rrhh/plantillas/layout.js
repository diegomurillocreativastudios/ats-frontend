export const metadata = {
  title: { absolute: "ATS | Plantillas" },
  description: "Gestión de plantillas de notificaciones",
};

export default function PlantillasLayout({ children }) {
  return children;
}
